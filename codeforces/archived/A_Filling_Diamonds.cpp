#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false); cin.tie(0); cout.tie(0);
    
    int g;
    cin >> g;
    unsigned long long a;
    for (int t = 0; t < g; t++) {
        cin >> a;
        cout << a << endl;
    }
}